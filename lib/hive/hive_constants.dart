// Box Names
const todoListBoxName = "todolist";
const routineListBoxName = "routines";

// Status Enumeration
enum TaskStatus {
  inProgress,
  active,
  done,
}
enum RoutineType {
  misc,
  school,
  home,
}